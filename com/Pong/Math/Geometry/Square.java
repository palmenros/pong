package com.Pong.Math.Geometry;

import com.Pong.Math.Number.Complex;

public class Square extends Rectangle {
    public Square(Complex position, double side) {
        super(position, side, side);
    }
}
